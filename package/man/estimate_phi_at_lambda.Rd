% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid_search_lambda.R
\name{estimate_phi_at_lambda}
\alias{estimate_phi_at_lambda}
\title{Estimate phi at a single lambda value}
\usage{
estimate_phi_at_lambda(
  current_data,
  lambda,
  functional_type,
  confidence_level,
  n_draws_from_F,
  n_future_studies_per_draw,
  innovation_type,
  epsilon_s,
  epsilon_y,
  delta_s_values,
  study_type,
  covariates
)
}
\arguments{
\item{current_data}{A tibble or data.frame with the current study data.
Must contain columns: A (treatment), S (surrogate), Y (outcome).}

\item{lambda}{Numeric. Single lambda value to evaluate.}

\item{functional_type}{Character. Type of functional to compute:
"correlation", "probability", or "conditional_mean".}

\item{confidence_level}{Numeric. Confidence level for this lambda (may be adjusted).}

\item{n_draws_from_F}{Integer. Number of outer bootstrap samples for
estimating phi. Larger values give more stable estimates but increase
computation time. Default: 500.}

\item{n_future_studies_per_draw}{Integer. Number of future studies to
generate per outer draw. Default: 200.}

\item{innovation_type}{Character. Type of innovation distribution:
"bayesian_bootstrap" (default) or "dirichlet_process".}

\item{epsilon_s}{Numeric. Threshold for probability functional (if used).}

\item{epsilon_y}{Numeric. Threshold for probability functional (if used).}

\item{delta_s_values}{Numeric vector. Values for conditional mean functional (if used).}

\item{study_type}{Character. Type of study for treatment effect estimation.}

\item{covariates}{Character vector. Covariates for adjustment.}
}
\value{
A list with: lambda, phi_hat, lower_ci, upper_ci, se
}
\description{
Internal helper function that estimates phi(F_lambda) for a single lambda.
}
\keyword{internal}
