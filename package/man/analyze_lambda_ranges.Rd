% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lambda_range_analysis.R
\name{analyze_lambda_ranges}
\alias{analyze_lambda_ranges}
\title{Analyze surrogate functionals across different λ ranges (DEPRECATED)}
\usage{
analyze_lambda_ranges(
  current_data,
  lambda_ranges = list(list(min = 0, max = 0.1), list(min = 0, max = 0.3), list(min = 0,
    max = 0.5), list(min = 0, max = 0.8), list(min = 0, max = 1)),
  n_draws_from_F = 200,
  n_future_studies_per_draw = 100,
  functional_type = c("correlation", "probability", "conditional_mean", "all"),
  epsilon_s = 0.2,
  epsilon_y = 0.1,
  delta_s_values = c(0.3, 0.5, 0.7),
  study_type = c("randomized", "observational"),
  covariates = NULL,
  seed = NULL
)
}
\arguments{
\item{current_data}{A tibble with the current study data.}

\item{lambda_ranges}{List of λ ranges to analyze. Each element should be a list
with 'min' and 'max' values. Default: c(0, 0.1), c(0, 0.3), c(0, 0.5), c(0, 0.8), c(0, 1.0).}

\item{n_draws_from_F}{Integer. Number of draws from super-population F.}

\item{n_future_studies_per_draw}{Integer. Number of future studies per draw from F.}

\item{functional_type}{Character. Type of functional to compute.}

\item{epsilon_s}{Numeric. Threshold for probability functional.}

\item{epsilon_y}{Numeric. Threshold for probability functional.}

\item{delta_s_values}{Numeric vector. Values for conditional mean functional.}

\item{study_type}{Character. Type of study for treatment effect estimation.}

\item{covariates}{Character vector. Covariates for adjustment.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A list similar to the output of \code{grid_search_lambda} for backward compatibility.
}
\description{
**This function is deprecated.** Use \code{\link{grid_search_lambda}} instead,
which implements the fixed-lambda framework from the revised methods paper.

The old approach sampled λ randomly from constrained Beta distributions,
which is inconsistent with the current paper's theoretical framework that
treats λ as a fixed design parameter. The new \code{grid_search_lambda}
evaluates phi(F_lambda) at fixed lambda values and finds the threshold λ*.
}
\details{
This function is maintained for backward compatibility but is deprecated.
It converts the old lambda_ranges specification into a lambda_grid and
calls \code{grid_search_lambda}.

**Migration guide:**
```r
# OLD CODE (deprecated):
result <- analyze_lambda_ranges(
  current_data,
  lambda_ranges = list(
    list(min = 0, max = 0.1),
    list(min = 0, max = 0.5)
  )
)

# NEW CODE (recommended):
result <- grid_search_lambda(
  current_data,
  lambda_grid = seq(0, 0.5, by = 0.05),
  threshold = 0.8
)
```
}
\seealso{
\code{\link{grid_search_lambda}} for the recommended approach
}
