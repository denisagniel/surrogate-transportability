% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_generators.R
\name{generate_study_data}
\alias{generate_study_data}
\title{Generate study data with mixture structure}
\usage{
generate_study_data(
  n,
  n_classes = 2,
  class_probs = rep(1/n_classes, n_classes),
  treatment_effect_surrogate,
  treatment_effect_outcome,
  surrogate_type = c("binary", "continuous"),
  outcome_type = c("binary", "continuous"),
  covariate_effects = list(surrogate = rep(0.3, n_classes), outcome = rep(0.1,
    n_classes)),
  noise_sd = 0.5,
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Sample size for the study.}

\item{n_classes}{Integer. Number of latent classes in mixture (default: 2).}

\item{class_probs}{Numeric vector. Probabilities for each latent class.
Must sum to 1. Default: c(0.5, 0.5).}

\item{treatment_effect_surrogate}{Numeric vector. Treatment effect on
surrogate for each latent class.}

\item{treatment_effect_outcome}{Numeric vector. Treatment effect on
outcome for each latent class.}

\item{surrogate_type}{Character. Type of surrogate: "binary" or "continuous".}

\item{outcome_type}{Character. Type of outcome: "binary" or "continuous".}

\item{covariate_effects}{List. Effects of covariates on surrogate and outcome.
Should contain elements 'surrogate' and 'outcome', each numeric vectors
of length n_classes.}

\item{noise_sd}{Numeric. Standard deviation of noise for continuous variables.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A tibble with columns:
  \item{class}{Latent class assignment (1 to n_classes)}
  \item{A}{Binary treatment assignment}
  \item{X}{Covariate (standard normal)}
  \item{S}{Surrogate marker}
  \item{Y}{Outcome}
}
\description{
Creates simulated study data with latent mixture structure for surrogate
marker evaluation. Supports both binary and continuous outcomes/surrogates.
}
\examples{
# Binary surrogate and outcome
data_binary <- generate_study_data(
  n = 500,
  treatment_effect_surrogate = c(0.3, 0.7),
  treatment_effect_outcome = c(0.2, 0.8),
  surrogate_type = "binary",
  outcome_type = "binary"
)

# Continuous surrogate and outcome
data_continuous <- generate_study_data(
  n = 500,
  treatment_effect_surrogate = c(0.5, 1.2),
  treatment_effect_outcome = c(0.3, 0.9),
  surrogate_type = "continuous",
  outcome_type = "continuous"
)

}
