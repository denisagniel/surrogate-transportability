% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytical_variance.R
\name{compute_analytical_ci}
\alias{compute_analytical_ci}
\title{Compute confidence interval using analytical variance}
\usage{
compute_analytical_ci(
  data,
  lambda = 0.3,
  phi_estimate,
  confidence_level = 0.95,
  n = nrow(data)
)
}
\arguments{
\item{data}{A data.frame with columns A, S, Y}

\item{lambda}{Numeric. Fixed perturbation distance}

\item{phi_estimate}{Numeric. Point estimate of φ(F_λ)}

\item{confidence_level}{Numeric. Confidence level (default 0.95)}

\item{n}{Integer. Sample size (default: nrow(data))}
}
\value{
A list with:
  \item{estimate}{Point estimate φ̂ₙ}
  \item{se}{Standard error: σ/√n}
  \item{ci_lower}{Lower confidence bound}
  \item{ci_upper}{Upper confidence bound}
  \item{variance_components}{Detailed variance components}
}
\description{
Computes asymptotic confidence intervals for φ̂ₙ(λ) using the
analytical variance from Proposition 1.
}
\details{
Under Proposition 1, √n(φ̂ₙ - φ) → N(0, σ²(λ)).
Therefore, φ̂ₙ ≈ N(φ, σ²(λ)/n) for large n.

The (1-α) confidence interval is:
φ̂ₙ ± z_{α/2} · σ(λ)/√n
}
\examples{
data <- generate_study_data(n = 500)
# Compute phi_estimate from actual inference
ci <- compute_analytical_ci(data, lambda = 0.3, phi_estimate = 0.7)

}
