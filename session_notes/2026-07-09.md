# Session Note: 2026-07-09 — Promote general theory to main text + external audit

## Summary
Promoted the general (bilinear-functional) asymptotic-normality theory from the
theory map into the paper: a **primary general theorem** in the main text
(Section 3) with a self-contained bilinear-functional proof in the appendix, the
existing finite-support/correlation result recast as an explicit **corollary**.
Then ran the external audit called for in the plan and fixed every finding.

## Key Changes
- **`inst/paper/proof_asymptotic_normality.tex`** — appended the general theory:
  A0 (conditional-effect transportability) + A5 (bilinear √n-estimability elbow
  `s_S+s_Y>d/2`); Lemmas A1/A3(core bilinear EIF)/A4(one-step)/A5(elbow)/A7(moment
  EIF); Theorem A (primary); Corollary A10 (finite-support collapse); post-proof
  audit. Conditions on the geometry (kernel `C` fixed), matching the existing proof.
- **`inst/paper/main.tex`** — Section 3 subsection "The general estimand and its
  two boundaries": general estimand, linear/bilinear reduction, A0/A5 boundaries,
  Theorem A statement + sketch, Corollary.
- **`inst/paper/refs.bib`** — 6 verified minimax citations (Bickel–Ritov,
  Birgé–Massart, Robins 2008/2017, Kennedy 2024, McClean 2024 = arXiv:2403.15175).

## Audit findings (all fixed)
- **proof-auditor: CONDITIONAL PASS, one Major** — eq (chi-ab) was labeled EIF but
  carried `−Θ_ab`, so it was not mean-zero. Fixed: EIF is now mean-zero as
  displayed; the `−Θ_ab` plug-in constant moved to the one-step plug-in term.
  Propagated through Step 4, the a=b diagonal line, Corollary A10, and confirmed
  Theorem A Step 3's mean-zero citation is now literally correct. Items 2–7 PASS.
- **domain-reviewer: SOUND, one Major** — body `C→Σ` lost the `p_0` factors. Fixed:
  stated `Σ_{kk'}=p_0(k)p_0(k')C(k,k')`. Minors fixed: "√n-consistent" qualifier;
  supplement A0 reworded to CATEs (not full conditional PO law); A-scheme
  cross-reference in body; `C_w` weight-bound; softened "can only absorb"; added
  Lemma A4 η-path clause and balanced-smoothness footnote.

## Decisions / Remaining Work
- Verified: `main.tex` compiles clean — 34pp, 0 undefined refs/citations, 0 fatal.
  All overfull hboxes are **pre-existing** (identical values in the committed HEAD
  baseline) wide appendix display equations; my edits added zero new ones and fixed
  the two body equations I owned (via `gathered`/`aligned`).
- Branch `theory/general-promotion`. Do **not** merge to main without user sign-off.
- Out of scope (flagged as future work): the unconditional kernel-differentiated
  (geometry-EIF) result; implementing the general/continuous-X estimator in the
  R package; filling the pending Table 2 numbers.

## Notes
Numeric sanity check (`explorations/theory_checks/`) confirmed the general χ_aa
collapses structurally to the finite-support `tr(ΣV)`, and it was that check that
surfaced the mean-zero/centering bookkeeping the auditor then confirmed.
