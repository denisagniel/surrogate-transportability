# Session Notes: 2026-07-08

## Goal
Bring the whole project (package, manuscript, sims, meta-docs) into line with the **canonical May 12 2026 presentation** (`inst/presentation/slides.qmd`), and audit prior work for correctness/deprecation.

## Approach
Session 1 = produce a master audit + realignment plan only (no code/paper edits). Saved to
`quality_reports/plans/2026-07-08_canonical-realignment-master-plan.md`.

## Key decisions (user)
- Deliverable this session: master plan document only.
- Deprecation posture: **DELETE** off-canonical code (git preserves it). Overrides the older May-1 "keep-but-flag" DEPRECATIONS.md.
- Correctness audit scope: **canonical pipeline only**.

## Key findings
1. **Canonical method** (from slides): TV-ball local geometry, uniform via hit-and-run MCMC (adaptive M≈2100), correlation of (ΔS,ΔY) across future studies, IF/delta-method inference, RCT importance weighting + observational cross-fitted AIPW, 4 linear/5-level-X DGPs, PTE-misleading example.
2. **Severe drift:** package has 38 R files / 81 exports, >50% orphaned. Competing minimax/Wasserstein/Bayesian implementations, all off-canonical.
3. **CRITICAL:** the canonical DGP `generate_dgp_data` is **not in the package** — defined inline & duplicated across 4+ scripts + cluster. Must be promoted into `R/`.
4. **`main.tex` drifted from its own PAPER_OUTLINE.md** — still leads with Wasserstein DRO, ε-close grid-search inference, RF-ensemble theorem, a "classification" Study 1, placeholder Studies 2–3, residual Bayesian content. Needs substantial rewrite to the (slide-aligned) outline.
5. **MEMORY.md is stale** — still describes the abandoned mixture model Q=(1−λ)P₀+λP̃ and `posterior_inference` as main function.

## Canonical live entry points
`tv_ball_correlation_IF_adaptive`, `generate_dgp_data` (needs promotion), `compute_pte` (comparison only).

## Next steps
- Await plan approval.
- Open questions in plan §9 (DGP framework reconciliation, R²/MSPE, PTE impl, old sim archival, IF-dependency check).
- Phase 1 = correctness audit (read-only) BEFORE any deletion.

## Open questions
See master plan §9 (Q1–Q5).

---

## Update: Phases 0, 1, 1.5 executed (2026-07-08)

**Phase 0** — Branched `canonical-realignment`, tagged `pre-realignment-2026-07-08`, committed full working-tree snapshot (220 files).

**Phase 1 (audit)** — Found the canonical pipeline itself is buggy (report: `quality_reports/2026-07-08_canonical-correctness-audit.md`). Decision: FIX FIRST (Phase 1.5), salvage-check before delete, derive IFs fresh.

**Phase 1.5 (fixes) — DONE:**
- TV-ball sampler: rewrote chord-finding analytically (was grid + seq(-10,10)); validated uniform vs rejection oracle.
- Influence functions: fixed AIPW centering (constant, not w·Δ) and RCT/IW arm denominators (×2 was e=0.5-only); documented in `inst/paper/derivation_influence_functions.md`.
- Proof: Markov-chain CLT, corrected rate (n=o(M)), 5-moment functional, non-degeneracy assumption; re-audited clean. Fixed pre-existing build break (main.tex missing `\newtheorem{assumption}`+`enumerate`, paragraph-in-proof). main.tex compiles (36pp).
- Calibration validated at n=3000 (SE/SD=0.97, 90% cov).

**Key caveat:** full 4-DGP validation at n=10,000 is a CLUSTER job (infeasible locally). Must re-run there with fixed estimator to confirm Table-2 coverage.

**Next:** Phase 2 (promote canonical DGP `generate_dgp_data` into R/, rewire scripts) — needs §9 Q1 confirmation.

---

## Update: Phases 2, 3 executed (2026-07-08)

**Phase 2** — Promoted canonical DGP into package: `R/dgp_canonical.R` (`generate_dgp_data` + `canonical_dgp_params` as single source of truth, replacing YAML+inline duplication; records dgp1/2/4/5 -> slides 1/2/3/4 mapping). Tests added; verified analytic rho matches stored values.

**Phase 3** — Deleted off-canonical code. R/: 38->8 files. NAMESPACE: 81->13 exports. Trimmed surrogate_functionals (correlation only) and traditional_methods (PTE/mediation/within-study; dropped classification helpers that sourced deleted files). DESCRIPTION rewritten to canonical method, deps pruned to stats/mgcv/ranger, v0.4.0. Removed superseded sims/, cluster/, slurm/ (Phase 2.5 replaces) and stale result figures. All tests pass.

**Decisions:** delete legacy DGP family (confirmed); regenerate cluster jobs with setup-cluster-simulations skill from ~/RAND/tools/agent-assisted-research-meta (Phase 2.5).

**Next:** Phase 2.5 (regenerate O2 sim study with fixed estimator) OR Phase 4 (manuscript realignment). Phase 2.5 is a prerequisite for the n=10,000 re-validation.

---

## Update: Phase 2.5 executed (2026-07-08)

Scaffolded simulations/canonical-validation/ using the setup-cluster-simulations skill (template-driven O2 infra from ~/RAND/tools/agent-assisted-research-meta). Wired to the package (generate_dgp_data + canonical_dgp_params, fixed tv_ball_correlation_IF_adaptive). Grid: 4 DGPs x 1000 reps x n=10000, lambda=0.3. Package installs cleanly + library() works; smoke test (n=400) passes for all 4 DGPs.

**To launch (user, on O2):** see simulations/README_O2.md + TRANSFER.md. profile_timing.R sizes reps/task; submit.sh mints run-id and chunks arrays; combine.R aggregates. This produces the n=10,000 re-validation confirming Table 2 coverage with the corrected estimator, and the data for regenerated figures.

**Next:** Phase 4 (manuscript realignment) or Phase 5 (meta-doc cleanup).

---

## Update: Phase 4 executed (2026-07-08)

Realigned inst/paper/main.tex to the canonical method. Structure: Introduction -> Setting -> TV-Ball Geometry -> Numerical studies -> Discussion (36pp -> 25pp). Deleted Wasserstein DRO, epsilon-close/grid-search inference, RF-ensemble theorem, closed-form concordance, TP/TN/FP/FN "classification" Study 1, Study 2/3 placeholders, Dirichlet/mixture residue. Added canonical 4-DGP study (Table 1 params pulled from canonical_dgp_params; correct gamma_A=1.0, non-uniform p_X, beta_SX), Table 2 marked PENDING cluster re-validation, PTE-misleading discussion (via DGPs 2/3/4), deferred-scope section. Retitled to "Evaluating Surrogate Transportability via Local Geometric Analysis".

Domain review run + all actionable findings fixed: RCT IF formula (propensity-normalized), unified estimand notation (Theta=Corr over mu; dropped Phi and the wrong E[phi(Q)] form), MCMC rate aligned to corrected proof, DGP2 mechanism corrected (beta_SX not gamma/beta_AX), removed untraceable PTE=0.54 numbers, fixed Assumption 3, removed "types"/Dirichlet residue. Compiles clean (25pp, 0 undefined refs).

**One documented gap:** Table 2 bias/coverage + Figures 1-3 pending the O2 cluster re-validation output (Phase 2.5).

**Next:** Phase 5 (meta-doc cleanup) then Phase 6 (final fidelity + merge report).
