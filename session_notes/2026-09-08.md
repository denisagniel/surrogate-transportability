# Session Notes — 2026-09-08

## 07:16 — 2026-09-08_main-tex-restructuring

**Goal.** `main.tex` has fallen well behind `theory.tex` (interior/saturated/exit-interior closed forms, χ² alternative geometry, two bias corrections all landed in `theory.tex` this week via commits `431ce55`/`ab90996` and `1eae3b5`/`02022bd`, but `main.tex` still carries all of it as one 34-line paragraph pointing at unnumbered "Supplementary Material" descriptions). Get outside perspective on how to restructure `main.tex` around the closed forms before touching any prose.

**What happened.** Ran three independent review agents synchronously against `main.tex`/`theory.tex` (structure-reviewer, domain-reviewer, librarian for external precedent). Background/parallel task launches hung (~21 min, no output, matching the known subprocess-spawn-wedge pattern) — cancelled and re-ran sequentially with tighter time-budget instructions, which fixed it. Worth remembering for future multi-agent fan-out in this environment.

Reviewers converged on: (1) promote the closed forms into real numbered content in `main.tex`'s body, proofs deferred to `theory.tex` by label; (2) a short "practical guidance" subsection as a table, placed before Discussion; (3) split the TV worked-example section's meaning (goes with the estimand content) from its implementation (merges into Estimation); (4) `main.tex` has no abstract at all — add one; (5) compress and move the old §2.3 (general-estimand/elbow-condition boundaries), whose title now collides with the new λ-regime "boundaries."

Reviewers disagreed on χ²'s placement: structure-reviewer wanted it promoted to a co-equal top-level section; domain-reviewer argued the current `theory.tex` χ² section (~230 lines) structurally mirrors TV's whole apparatus and reads as "two geometries to survey" rather than insight — the sharper, narrower claim is that the closed-form endpoint is a property of the *distance*, not the estimand (TV's two endpoints coincide; χ²'s don't), which is one subsection, not a parallel section. Resolved in favor of domain-reviewer's framing (also matches librarian's Duchi–Namkoong 2021 precedent of folding multiple f-divergence instances into one properties subsection).

Domain-reviewer also surfaced a live constraint on the guidance subsection: the "compute both anchors, skip the sweep if they agree" heuristic discussed earlier this week is not licensed by the paper's own DGP1 numbers — the anchor gap (`|Corr_K - Corr_{P0}| = 0.0044`) is ~10x smaller than the actual λ=0.3 excursion (`0.045`), so anchor agreement doesn't predict excursion size. The plan below states the workflow as an explicit proposal pending stress-testing, not a proven rule, and changes the actual decision criterion to excursion-vs-CI-half-width instead of anchor disagreement.

Five independent correctness bugs also surfaced, listed in the plan regardless of restructuring timing: (a) `main.tex` still calls χ² "future work" while `theory.tex` has already proven it; (b) the old TV-implementation section says Θ is "the sample mean over MCMC draws," directly contradicting §2.2's own statement that this is wrong for a nonlinear functional; (c) six `\textbf{Assumption...}\label{...}` pseudo-headers likely have `\ref`s resolving to section numbers rather than assumption numbers, since the defined `assumption` environment is unused; (d) Table 2's caption still has bracketed placeholder/provenance text; (e) 9 unreferenced labels.

Wrote the outline-level restructuring plan to `quality_reports/plans/2026-09-08_main-tex-restructuring.md` (gitignored, local only) with a full proposed section table, the converged/disagreement resolution above, the five bug fixes, and a Phase A (prose/structure only) vs. Phase B (new computation: adversarial DGP, Θ(λ)-sweep figures with CI bands, filling the still-`[PENDING]` Table 2, validating the real guidance criterion, jackknife write-up) split. Phasing order not decided — left for the user.

**Decisions.**
- χ² gets one subsection folded into the closed-forms section, not a parallel top-level section (domain-reviewer's framing over structure-reviewer's).
- The anchor-disagreement heuristic will not be written into `main.tex` as a proven rule; it goes in as an explicitly hedged proposal, with the decision criterion changed to excursion-size-vs-CI-half-width.
- Nothing in `main.tex`/`theory.tex` was edited this session — plan only, pending user sign-off.

**Next.** User asked to pick this up again next session with a review of the outline itself. Open questions carried forward: (1) does the proposed section table look right, or need changes; (2) Phase A vs. Phase B sequencing — restructure prose first around not-yet-validated claims, or land the Table 2/adversarial-DGP evidence first. Neither Oracle nor Momus has reviewed this plan yet; likely worth a Momus pass before Phase A drafting begins given the scale (most of a 774-line manuscript), but that's the user's call.
