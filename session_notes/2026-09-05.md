# Session Notes — 2026-09-05

## 07:41 — TV-choice audit, chi-squared closed form, and the exit-interior-regime theorem

**Goal.** Continuation of 2026-09-04's canonical-realignment thread. Three asks in sequence: (1) commit yesterday's checkpoint + resume the Sigma_0/a_bar perturbation-bound numerical thread; (2) assess how much theoretical work the TV-distance choice is doing in `theory.tex`/`main.tex`, and whether a different distance could be substituted; (3) develop and analytically explain a "sensitivity value" summary for lambda (`Rosenbaum-Gamma*`/E-value analog), which led into deriving and drafting a genuinely new theorem.

**What happened — commit + Sigma_0 thread.** Committed the 2026-09-04 checkpoint (`theory.tex` interior/saturated/sieve-limit realignment + the `debiased`/`jackknife` bias-correction implementation) on branch `theory/interior-regime-and-bias-correction`, merged `--no-ff` into `main` (no GitHub PR — `gh` unusable in this sandbox, config permission error). 22 new tests pass, full suite clean. Deliberately left out unrelated 2026-08-21 fixes and config drift for a separate commit. Resumed the Sigma_0 = a_bar*P_perp + Sigma_0 perturbation-bound check from the prior session: the scalar `||Sigma_0||_op/a_bar` does NOT usefully predict the Corr_K-vs-Theta gap (Cauchy-Schwarz bound ~955x looser than the true effect); the exact first-order bilinear term explains 68% of the gap. Not promoted to theory.tex — logged as a negative/partial finding.

**What happened — TV-vs-other-distances audit (Oracle-consulted).** Asked how much work the TV-distance choice does. Oracle's verdict: the estimation/inference theory (Theorem `thm:main`, AIPW, general bilinear EIF machinery) is fully distance-agnostic — TV does zero work there. The interior-regime closed form IS geometry-specific, but not uniquely TV's: chi-squared distance has its OWN clean interior closed form (via a whitening substitution turning the weighted ellipsoid into a sphere), giving `Theta_chisq = Corr_{P0}(tau_S,tau_Y)` — the P0-weighted correlation, which is exactly the unlabeled "0.6411" number already sitting in `theory.tex`'s `ex:binding` example. TV is the unique f-divergence that is simultaneously an intrinsic divergence AND an unweighted norm of the perturbation — that's the real reason it's special, not arbitrary convenience. Fixed a real, unrelated bug this surfaced: `main.tex`'s hit-and-run algorithm description had a bogus "project to simplex" step 4 that contradicts the actual (exact, no-projection-needed) sampler; rewrote that paragraph to match `R/tv_ball_sampling.R`.

**What happened — lambda sensitivity-value (rejected), then the real analytic result.** User picked "report a Rosenbaum-Gamma*/E-value-style sensitivity value" from a menu of lambda-selection strategies. Numerically swept Theta(lambda) for DGP1 and found it is NOT monotone — it humps upward (peak ~+0.046 above Corr_K near lambda=0.25) then returns to Corr_K exactly at both regime endpoints (proved in `theory.tex` already), which structurally breaks any single-crossing "tipping point" framing (Oracle: the estimand can't distinguish "no shift" from "total ignorance," unlike Gamma* where Gamma->infinity is genuinely unbounded). Replaced the sensitivity-value idea with a request to understand the mechanism analytically instead.

Derived (with heavy Oracle back-and-forth, then independently re-verified every step by hand before writing anything) a genuinely new theorem: **as lambda crosses `p_min := min_k p0(k)`, the smallest-mass cell's nonnegativity constraint cuts a facet (not a vertex) off the TV-ball's root-polytope, and the first-order slope of Theta(lambda) at that point equals (a positive, K-dependent constant) times minus the classical Pearson-correlation influence function, evaluated at the smallest stratum's standardized CATEs.** I.e., widening the ball past the interior regime helps Theta iff the smallest covariate stratum is a net *discordant* contributor to the baseline correlation, hurts it iff concordant — an MCMC-free sign diagnostic. Validated numerically: designed 4 configurations (agree-on-large/disagree-on-small and its mirror, DGP1's tied-minimizer case, and a unique-minimizer/large-p_min control), swept lambda in the narrow window `(p_min, 1.3*p_min)` with common random numbers, and got the predicted sign in all 4 cases with a single fitted constant `C_K` matching to within 30% across wildly different designs (also independently found a real, unrelated design mistake in the first numerical attempt — a `tau_Y` with zero variance — before this).

**Drafted into `theory.tex`** as new Section 3.4 ("Exiting the interior regime: a first-order account"): Lemma on `B_lambda`'s root-polytope vertex/facet structure (type-A_{K-1}, cite Postnikov 2009), Lemma on exact global constancy when P0 is uniform (a clean corollary of the same machinery), Proposition on the first-order exit slope with the influence-function identity (cite Devlin-Gnanadesikan-Kettenring 1975), plus remarks on scope (local result only, not the whole intermediate band) and on the corrected kink structure (kinks occur at every distinct *subset sum* of `p0`, not at every individual `p0(k)` as I'd loosely claimed in conversation — up to `2^K-2` of them, with the two regime-boundary thresholds as the extreme subset sums). Propagated pointers into `main.tex`'s "What does lambda mean?" and "Interpreting lambda" paragraphs.

**Two rounds of real bugs caught before this was safe to leave.** (1) A self-inflicted editing bug: my first insertion of the new appendix proof accidentally deleted the `\subsection{Proof of Lemma~\ref{lem:cancellation}}` heading that followed it (the anchor text used to find the insertion point wasn't fully restored in the replacement) — caught by noticing an orphaned `\begin{proof}` with no heading, fixed by restoring it. (2) A full Oracle proof-audit of the actual written LaTeX (not just the underlying derivation) found ten real issues: a one-sided-inequality error in the vertex-extremality proof (needed both the upper and lower triangle-inequality bounds, and the "u_i=w_i=lambda" step as originally written was simply false — `(2lambda,0)` also satisfies the equation used), a wrong subset argument in Step 1 (`S={k}` instead of `S=[K]\{k}`), a false "0 otherwise" claim in the facet lemma (other vertices are `0` OR `+lambda`, not just `0`), an unjustified/wrong inclusion-exclusion argument for tied minimizers (replaced with a cleaner exact-disjointness proof once noticed that tied cells' removed regions literally cannot overlap), a wrong citation for why `sum Psi(k)=0`, an overclaim ("recovering" softened to "consistent with, not a proof of"), a missing `E[Z]=0` justification, and — most importantly — **four symbol collisions with existing `theory.tex` notation** (my new `Psi(k)` collided with the paper's already-load-bearing Layer-2 functional `Psi`; `m` collided with the moment vector; `kappa` collided with the MCMC variance constant; `r` collided with the effective-rank symbol from `sec:rank`). Renamed throughout (`Psi->varpi`, `m->p_min`, `kappa->kappa_vol`, `r->r_K`), and properly introduced the previously-unnamed projection matrix `P_H := I_K - (1/K)*11^T` (used unnamed in ten places in the existing proof of `prop:interior`) without touching that already-audited proof text.

**Verification.** Both `theory.tex` (34 pages) and `main.tex` (15 pages) compile clean end-to-end (xelatex x3 + bibtex where applicable): no undefined references, no undefined citations, no "labels may have changed" warnings on the final pass, no new overfull hboxes introduced by any of today's edits (fixed two minor ones that did appear, by moving dense inline formulas into display equations / shortening a lemma title). `\begin{proof}`/`\end{proof}` counts balanced (21/21).

**Not yet done.** None of today's new theorem content has been committed yet (still local, uncommitted, alongside the unrelated pre-existing Aug-21 fixes and config drift noted in 2026-09-04's note). The two exploration scripts from today (`2026-09-05_theta-lambda-downward-search.R`, `-v2.R`, `2026-09-05_uniform-p0-check.R`, `2026-09-05_local-slope-validation.R`) are throwaway numerics, not part of the reproducible artifact set.

**Next.** Commit today's `theory.tex`/`main.tex` changes plus the exploration scripts. Consider whether Corollary~\ref{cor:interior-eif} interacts with the new Proposition~\ref{prop:exit-interior} (both concern behavior right at `lambda=p_min`) and cross-reference if so. The chi-squared interior-regime closed form (Section on TV-vs-other-distances) was analytically sketched but not drafted into `theory.tex` — still open, lower priority than the exit-interior theorem that displaced it.

