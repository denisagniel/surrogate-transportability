% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserstein_minimax.R
\name{sample_wasserstein_perturbation}
\alias{sample_wasserstein_perturbation}
\title{Sample Perturbation in Wasserstein Ball}
\usage{
sample_wasserstein_perturbation(
  reference,
  cost_matrix,
  radius,
  method = c("normal", "dirichlet", "uniform"),
  max_attempts = 50
)
}
\arguments{
\item{reference}{Numeric vector (length J): reference type distribution (p0)}

\item{cost_matrix}{Matrix (J x J): pairwise cost between types}

\item{radius}{Numeric: Wasserstein ball radius (lambda_w)}

\item{method}{Character: sampling method ("normal", "dirichlet", "uniform")}

\item{max_attempts}{Integer: maximum rejection sampling attempts}
}
\value{
Numeric vector (length J) on probability simplex satisfying W_2 constraint
}
\description{
Generates a random type distribution q satisfying W_2(q, p0) <= lambda_w
using constrained sampling with random directions.
}
\details{
Generates random perturbations by:

1. Sample random direction delta
2. Find maximum step size tau such that q = p0 + tau*delta satisfies:
   - W_2(q, p0) <= radius
   - q on probability simplex (sum = 1, q >= 0)
3. Return q

**Sampling methods:**
- **normal**: delta ~ N(0, I), then zero-centered
- **dirichlet**: delta = Dirichlet(1,...,1) - p0
- **uniform**: delta = uniform on sphere, then zero-centered

Uses binary search to find the maximum feasible step size, ensuring
efficient exploration of the Wasserstein ball boundary.
}
\keyword{internal}
