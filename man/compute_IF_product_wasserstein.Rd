% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserstein_minimax_IF_inference.R
\name{compute_IF_product_wasserstein}
\alias{compute_IF_product_wasserstein}
\title{Compute Doubly Robust IF for Product h(X) = τ_S(X) · τ_Y(X) (Internal)}
\usage{
compute_IF_product_wasserstein(
  obs,
  tau_S,
  tau_Y,
  mu_S1,
  mu_S0,
  mu_Y1,
  mu_Y0,
  e
)
}
\arguments{
\item{obs}{Single observation (row from data frame)}

\item{tau_S}{Estimated τ_S(X) for this observation}

\item{tau_Y}{Estimated τ_Y(X) for this observation}

\item{mu_S1}{Estimated E[S|A=1,X] for this observation}

\item{mu_S0}{Estimated E[S|A=0,X] for this observation}

\item{mu_Y1}{Estimated E[Y|A=1,X] for this observation}

\item{mu_Y0}{Estimated E[Y|A=0,X] for this observation}

\item{e}{Propensity score e(X) = P(A=1|X) for this observation}
}
\value{
Numeric: Doubly robust IF value for product
}
\description{
Computes the efficient influence function for E[τ_S(X) · τ_Y(X)] using
doubly robust corrections. Based on the EIF derivation in functional_cate_covariance.R.
}
\details{
This is the proper EIF for the product functional, NOT the simplified version
τ_S · IF_Y + τ_Y · IF_S (which is only correct under special conditions).
}
\keyword{internal}
