% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tv_ball_correlation_IF_adaptive.R
\name{tv_ball_correlation_IF_adaptive}
\alias{tv_ball_correlation_IF_adaptive}
\title{TV Ball Correlation with Adaptive M (IF-Based Inference)}
\usage{
tv_ball_correlation_IF_adaptive(
  data,
  lambda,
  M_start = 300,
  M_increment = 300,
  M_max = 5000,
  tolerance = 0.01,
  n_stable = 3,
  burn_in = 500,
  thin = 5,
  alpha = 0.05,
  method = c("bootstrap", "importance_weighting"),
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame with X, A, S, Y}

\item{lambda}{TV ball radius}

\item{M_start}{Initial number of future studies (default: 300)}

\item{M_increment}{How much to increase M each iteration (default: 300)}

\item{M_max}{Maximum M to try (default: 5000)}

\item{tolerance}{Convergence tolerance (default: 0.01)}

\item{n_stable}{Number of consecutive stable iterations required (default: 3)}

\item{burn_in}{MCMC burn-in}

\item{thin}{MCMC thinning}

\item{alpha}{Significance level}

\item{method}{"bootstrap" or "importance_weighting"}

\item{verbose}{Print progress?}
}
\value{
List with rho_hat, se, ci_lower, ci_upper, IF_vals, and convergence info
}
\description{
Automatically increases M until correlation estimate stabilizes.
}
\details{
The algorithm:
1. Start with M = M_start
2. Compute ρ̂(M)
3. Increase M by M_increment
4. Compute ρ̂(M + M_increment)
5. Check convergence: ALL of the following must hold:
   - Each consecutive change |ρ̂_t - ρ̂_{t-1}| < tolerance
   - Overall change |ρ̂_t - ρ̂_{t-n_stable}| < tolerance
   - This must hold for n_stable consecutive iterations
6. If converged, stop. Otherwise, repeat from step 3 (up to M_max)

This prevents both premature convergence and consistent drift.
}
