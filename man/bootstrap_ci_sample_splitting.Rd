% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_splitting_minimax_wasserstein.R
\name{bootstrap_ci_sample_splitting}
\alias{bootstrap_ci_sample_splitting}
\title{Bootstrap Confidence Interval for Sample Splitting Minimax}
\usage{
bootstrap_ci_sample_splitting(
  data,
  covariates,
  lambda_w,
  split_ratio = 0.5,
  tau_method = c("kernel", "rf", "gam", "linear"),
  cross_fit = TRUE,
  cost_function = c("euclidean", "manhattan"),
  scale_covariates = TRUE,
  n_bootstrap = 500,
  confidence_level = 0.95,
  seed = NULL,
  parallel = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame with A, S, Y, and covariates}

\item{covariates}{Character vector: covariate names}

\item{lambda_w}{Numeric: Wasserstein radius}

\item{split_ratio}{Numeric: proportion for D1}

\item{tau_method}{Character: treatment effect estimation method}

\item{cross_fit}{Logical: use cross-fitting within splits?}

\item{cost_function}{Character: distance metric}

\item{scale_covariates}{Logical: standardize covariates?}

\item{n_bootstrap}{Integer: number of bootstrap samples}

\item{confidence_level}{Numeric: confidence level (default 0.95)}

\item{seed}{Integer: random seed for main split (NULL = no seed)}

\item{parallel}{Logical: use parallel processing?}

\item{verbose}{Logical: print progress?}
}
\value{
List with phi_star, ci_lower, ci_upper, bootstrap_estimates,
  n_d1, n_d2, optimal_gamma_d1
}
\description{
Computes bootstrap CI by resampling D2 only (no selection bias).
}
\details{
**Bootstrap Strategy:**
1. Split original data once: D1, D2
2. Find gamma* on D1 (fixed across bootstrap)
3. Bootstrap D2 only (resample with replacement)
4. For each bootstrap sample, estimate concordance using gamma* from D1
5. Percentile CI from bootstrap distribution

**Why this works:**
- Selection (gamma*) happens once on D1
- Bootstrap resamples D2 only
- Each bootstrap sample is independent of selection
- Standard bootstrap theory applies (no post-selection bias)
}
\examples{
\dontrun{
result <- bootstrap_ci_sample_splitting(
  data = mydata,
  covariates = c("X1", "X2"),
  lambda_w = 0.5,
  n_bootstrap = 500
)

cat(sprintf("Estimate: \%.4f\n", result$phi_star))
cat(sprintf("95\%\% CI: [\%.4f, \%.4f]\n",
            result$ci_lower, result$ci_upper))
}

}
