% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chisq_ball_sampling.R
\name{find_feasible_range_chisq}
\alias{find_feasible_range_chisq}
\title{Find Feasible Range Along a Direction in the Whitened Chi-Squared Ball (Internal)}
\usage{
find_feasible_range_chisq(u_current, direction, nu, lambda)
}
\arguments{
\item{u_current}{Numeric vector. Current whitened point (in H', norm² ≤ λ).}

\item{direction}{Numeric vector. Unit direction in H'.}

\item{nu}{Numeric vector. `sqrt(P0)`; the box floor is `-nu`.}

\item{lambda}{Numeric. Chi-squared radius.}
}
\value{
List with elements:
  \item{t_min}{Lower bound of feasible range}
  \item{t_max}{Upper bound of feasible range}
}
\description{
Given the current whitened point `u` and a unit direction `d` in H', find
`[t_min, t_max]` such that `u + t*d` satisfies
1. the χ² constraint `‖u + t d‖² ≤ λ` (one smooth quadratic), and
2. nonnegativity `(u + t d)_k ≥ -ν_k` (equivalently `q_k ≥ 0`).
}
\details{
No breakpoint enumeration is needed: unlike TV, the χ² radius is a single
quadratic in `t`, solved in closed form.
}
\keyword{internal}
