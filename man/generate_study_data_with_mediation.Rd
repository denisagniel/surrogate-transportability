% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_generators_corrected.R
\name{generate_study_data_with_mediation}
\alias{generate_study_data_with_mediation}
\title{Generate study data WITH controlled mediation (alternative approach)}
\usage{
generate_study_data_with_mediation(
  n,
  n_classes = 2,
  class_probs = rep(1/n_classes, n_classes),
  treatment_effect_surrogate,
  treatment_effect_outcome_TOTAL,
  surrogate_outcome_coefficient = 0.5,
  surrogate_type = c("binary", "continuous"),
  outcome_type = c("binary", "continuous"),
  covariate_effects = list(surrogate = rep(0.3, n_classes), outcome = rep(0.1,
    n_classes)),
  noise_sd = 0.5,
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Sample size.}

\item{n_classes}{Integer. Number of latent classes.}

\item{class_probs}{Numeric vector. Class probabilities.}

\item{treatment_effect_surrogate}{Numeric vector. Treatment effect on S.}

\item{treatment_effect_outcome_TOTAL}{Numeric vector. TOTAL treatment effect on Y
(including both direct effect and indirect effect through S).}

\item{surrogate_outcome_coefficient}{Numeric. Causal effect of S on Y (β in Y = ... + βS).
Default: 0.5.}

\item{surrogate_type}{Character. "binary" or "continuous".}

\item{outcome_type}{Character. "binary" or "continuous".}

\item{covariate_effects}{List. Covariate effects on S and Y.}

\item{noise_sd}{Numeric. Noise standard deviation.}

\item{seed}{Integer. Random seed.}
}
\value{
A tibble with columns: class, A, X, S, Y, and additional columns:
  \item{te_y_direct}{Direct treatment effect on Y (for each class)}
  \item{te_y_indirect}{Indirect effect through S (for each class)}
  \item{te_y_total}{Total effect (for verification)}
}
\description{
This version allows S to cause Y, but requires specifying TOTAL treatment
effects on Y, which are then decomposed into direct and indirect (through S).
}
\details{
Use this when you want to model S as a causal mediator but still control
the total treatment effect on Y.
}
\examples{
# Opposite effects: treatment increases S but decreases Y
opposite_effects <- generate_study_data_with_mediation(
  n = 500,
  treatment_effect_surrogate = c(0.5, 0.5),
  treatment_effect_outcome_TOTAL = c(-0.3, -0.3),  # Negative total effect
  surrogate_outcome_coefficient = 0.7
)

}
